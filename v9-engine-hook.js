'use strict';

// RunFlow V9 / Athlete V2 isolated API layer.
// Loaded before server.js. It only handles /api/v9/* and /api/v2/* routes.

const http = require('http');
const crypto = require('crypto');
const fs = require('fs');
const path = require('path');
const zlib = require('zlib');
const { URL } = require('url');

const SUPABASE_URL = String(process.env.SUPABASE_URL || '').replace(/\/$/, '');
const SUPABASE_ANON_KEY = String(process.env.SUPABASE_ANON_KEY || '');
const SUPABASE_SERVICE_ROLE_KEY = String(process.env.SUPABASE_SERVICE_ROLE_KEY || '');
const APP_ENCRYPTION_KEY = String(process.env.APP_ENCRYPTION_KEY || '');
const INTERVALS_API_BASE = 'https://intervals.icu/api/v1';
const IS_PROD = process.env.NODE_ENV === 'production';
const DATA_DIR = path.join(__dirname, 'data');
const BANNER_FIELD = 'RUNFLOW_MOTIVATION_BANNER';
const LOAD_HISTORY_FIELD = 'RUNFLOW_MAX_LOAD_HISTORY';

function parseCookies(req){const out={};String(req.headers.cookie||'').split(';').forEach(part=>{const i=part.indexOf('=');if(i<0)return;const k=part.slice(0,i).trim(),v=part.slice(i+1).trim();if(k)out[k]=decodeURIComponent(v)});return out}
function cookie(name,value,maxAge){return `${name}=${encodeURIComponent(value)}; Path=/; HttpOnly; SameSite=Lax${IS_PROD?'; Secure':''}; Max-Age=${maxAge}`}
function sendJson(res,status,data){const body=JSON.stringify(data);res.writeHead(status,{'Content-Type':'application/json; charset=utf-8','Content-Length':Buffer.byteLength(body),'Cache-Control':'no-store','X-Content-Type-Options':'nosniff'});res.end(body)}
async function readJson(req){let body='';for await(const chunk of req){body+=chunk;if(body.length>2000000)throw Object.assign(new Error('Solicitud demasiado grande.'),{status:413})}if(!body)return{};try{return JSON.parse(body)}catch{throw Object.assign(new Error('JSON no válido.'),{status:400})}}
async function fetchJson(url,options={}){const response=await fetch(url,options),text=await response.text();let data=null;try{data=text?JSON.parse(text):null}catch{data=text}if(!response.ok){const msg=data&&typeof data==='object'?(data.message||data.error_description||data.error||data.details):data;throw Object.assign(new Error(msg||`HTTP ${response.status}`),{status:response.status,details:data})}return data}
async function sb(table,query='',options={}){if(!SUPABASE_URL||!SUPABASE_SERVICE_ROLE_KEY)throw Object.assign(new Error('Supabase no está configurado.'),{status:503});const headers={apikey:SUPABASE_SERVICE_ROLE_KEY,Authorization:`Bearer ${SUPABASE_SERVICE_ROLE_KEY}`,...(options.body!==undefined?{'Content-Type':'application/json'}:{}),...(options.prefer?{Prefer:options.prefer}:{})};return fetchJson(`${SUPABASE_URL}/rest/v1/${table}${query?`?${query}`:''}`,{method:options.method||'GET',headers,body:options.body===undefined?undefined:JSON.stringify(options.body)})}
async function authUser(access){return fetchJson(`${SUPABASE_URL}/auth/v1/user`,{headers:{apikey:SUPABASE_ANON_KEY,Authorization:`Bearer ${access}`}})}
async function authRefresh(refresh){return fetchJson(`${SUPABASE_URL}/auth/v1/token?grant_type=refresh_token`,{method:'POST',headers:{apikey:SUPABASE_ANON_KEY,'Content-Type':'application/json'},body:JSON.stringify({refresh_token:refresh})})}
async function requireSession(req,res){const cookies=parseCookies(req);let access=cookies.rf_access,refresh=cookies.rf_refresh;if(!access)throw Object.assign(new Error('Debes iniciar sesión.'),{status:401});let user;try{user=await authUser(access)}catch{if(!refresh)throw Object.assign(new Error('Debes iniciar sesión.'),{status:401});const renewed=await authRefresh(refresh);access=renewed.access_token;refresh=renewed.refresh_token||refresh;user=renewed.user||await authUser(access);res.setHeader('Set-Cookie',[cookie('rf_access',access,Math.max(60,Number(renewed.expires_in)||3600)),cookie('rf_refresh',refresh,60*60*24*30)])}const roles=await sb('user_roles',`user_id=eq.${encodeURIComponent(user.id)}&select=role`);const athletes=await sb('athletes',`user_id=eq.${encodeURIComponent(user.id)}&lifecycle_status=eq.active&select=id&limit=1`);return{user,roles:roles.map(r=>r.role),athlete_id:athletes[0]?.id||null}}
function requireRole(session,role){if(!session.roles.includes(role))throw Object.assign(new Error('No tienes permiso para realizar esta acción.'),{status:403})}
async function ensureCoachAccess(session,athleteId){requireRole(session,'coach');const rows=await sb('coach_athletes',`coach_user_id=eq.${encodeURIComponent(session.user.id)}&athlete_id=eq.${encodeURIComponent(athleteId)}&select=athlete_id&limit=1`);if(!rows.length)throw Object.assign(new Error('No tienes acceso a este deportista.'),{status:403})}
function safeText(v,max=2000){return String(v??'').trim().slice(0,max)}
function num(v,min=-Infinity,max=Infinity){const n=Number(v);return Number.isFinite(n)&&n>=min&&n<=max?n:null}
function validDate(v){return /^\d{4}-\d{2}-\d{2}$/.test(String(v||''))?String(v):null}
function addDays(iso,n){const d=new Date(`${iso}T12:00:00Z`);d.setUTCDate(d.getUTCDate()+n);return d.toISOString().slice(0,10)}
function localDate(){return new Intl.DateTimeFormat('en-CA',{timeZone:process.env.RUNFLOW_TIMEZONE||'Europe/Madrid',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())}
function safeObj(v){return v&&typeof v==='object'&&!Array.isArray(v)?v:{}}
function percentile(values,p){const a=(values||[]).map(Number).filter(Number.isFinite).sort((x,y)=>x-y);if(!a.length)return null;if(a.length===1)return a[0];const pos=(a.length-1)*p,lo=Math.floor(pos),hi=Math.ceil(pos);return lo===hi?a[lo]:a[lo]+(a[hi]-a[lo])*(pos-lo)}
function clamp(v,min,max){return Math.min(max,Math.max(min,v))}
function sportKey(value){const v=String(value||'').toLowerCase();if(/ride|bike|cycling|bici/.test(v))return'bike';if(/trail/.test(v))return'trail';if(/run|carrera/.test(v))return'run';if(/strength|weight|fuerza/.test(v))return'strength';if(/walk|hike|paseo|sender/.test(v))return'walk';return'other'}
function availabilityMode(day,profile){const explicit=String(day?.activity_type||day?.mode||'');if(['unavailable','run','trail','bike','strength','gym','flexible'].includes(explicit))return explicit;if(day?.can_train===false)return'unavailable';if(day?.gym&&day?.strength&&day?.run===false)return'gym';if(day?.strength&&day?.run===false&&day?.bike!==true)return profile?.strength_mode==='external'?'gym':'strength';if(day?.bike&&day?.run===false&&day?.strength!==true)return'bike';if(day?.run&&day?.mountain)return'trail';if(day?.run&&day?.bike!==true&&day?.strength!==true)return'run';return'flexible'}
function workoutKind(workout){const text=String(`${workout?.sport||''} ${workout?.title||''} ${workout?.adaptation_target||''}`).toLowerCase();if(workout?.is_strength||/strength|fuerza|gimnasio/.test(text))return'strength';if(/ride|bike|cycling|bici/.test(text))return'bike';if(/trail|montaña|mountain|desnivel/.test(text))return'trail';return'run'}
function dayAllowsWorkout(day,workout,profile){if(!day)return true;const mode=availabilityMode(day,profile),kind=workoutKind(workout);if(mode==='unavailable'||day.can_train===false)return false;if(mode==='run')return kind==='run';if(mode==='trail')return kind==='trail';if(mode==='bike')return kind==='bike';if(mode==='strength'||mode==='gym')return kind==='strength';if(mode==='flexible')return kind==='strength'?day.strength!==false:kind==='bike'?day.bike===true:day.run!==false;return false}

function encryptionKey(){if(!APP_ENCRYPTION_KEY||APP_ENCRYPTION_KEY.length<24)return null;try{const raw=Buffer.from(APP_ENCRYPTION_KEY,'base64');if(raw.length===32)return raw}catch{}return crypto.createHash('sha256').update(APP_ENCRYPTION_KEY,'utf8').digest()}
function decryptSecret(record){const key=encryptionKey();if(!key||!record)return null;const decipher=crypto.createDecipheriv('aes-256-gcm',key,Buffer.from(record.secret_iv,'base64'));decipher.setAuthTag(Buffer.from(record.secret_tag,'base64'));return Buffer.concat([decipher.update(Buffer.from(record.secret_ciphertext,'base64')),decipher.final()]).toString('utf8')}
async function intervalsKey(athleteId){const rows=await sb('athlete_integrations',`athlete_id=eq.${encodeURIComponent(athleteId)}&provider=eq.intervals&select=*&limit=1`).catch(()=>[]);return rows[0]?decryptSecret(rows[0]):null}
async function intervalsFetch(apiKey,endpoint,options={}){return fetchJson(`${INTERVALS_API_BASE}${endpoint}`,{...options,headers:{Authorization:`Basic ${Buffer.from(`API_KEY:${apiKey}`).toString('base64')}`,Accept:'application/json',...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})}})}

let libraryCache=null;
function loadLibrary(){if(libraryCache)return libraryCache;let encoded='';const single=path.join(DATA_DIR,'runflow-v9-library.b64');if(fs.existsSync(single))encoded=fs.readFileSync(single,'utf8').trim();else{const candidates=fs.readdirSync(DATA_DIR).filter(n=>/^runflow-v9-library\.part\d+\.b64$/.test(n)).sort((a,b)=>Number(a.match(/part(\d+)/)[1])-Number(b.match(/part(\d+)/)[1]));encoded=candidates.map(n=>fs.readFileSync(path.join(DATA_DIR,n),'utf8').trim()).join('')}if(!encoded)throw Object.assign(new Error('Biblioteca V9 no disponible.'),{status:503});const compact=JSON.parse(zlib.gunzipSync(Buffer.from(encoded,'base64')).toString('utf8'));const keyMap={i:'ID',n:'Nombre',d:'Disciplina',f:'Familia',o:'Objetivo_primario',ph:'Fases',g:'Objetivos_prueba',lv:'Nivel_1_5',tm:'Tiempo_total_min',wu:'Calentamiento',bp:'Bloque_principal',cd:'Vuelta_calma',int:'Intensidad_prescrita',r:'RPE_sesion_estimado',ter:'Terreno',e:'Desnivel_requerido',mat:'Material',rec:'Recuperacion_recomendada_h',pr:'Progresion_anterior_ID',nx:'Progresion_siguiente_ID',cg:'Grupo_comparacion',cmp:'Comparabilidad',sel:'Condiciones_seleccion',no:'No_usar_si',ok:'Criterio_exito',adj:'Regla_ajuste',l:'Carga_sRPE_est',lr:'Carga_relativa_1_5'};libraryCache={version:'Biblioteca RunFlow de Sesiones v1',sessions:(compact.s||[]).map(row=>Object.fromEntries(Object.entries(row).map(([k,v])=>[keyMap[k]||k,v])))};return libraryCache}

function activityDurationMin(a){const sec=num(a.duration_sec,0);if(sec!==null)return sec/60;const km=num(a.distance_m,0)!==null?Number(a.distance_m)/1000:null,speed=activitySpeedKmh(a);return km&&speed?(km/speed)*60:null}
function activitySpeedKmh(a){const raw=safeObj(a.raw_summary),mps=num(raw.average_speed,0)??num(raw.avg_speed,0);if(mps!==null&&mps>0)return mps*3.6;const km=num(a.distance_m,0)!==null?Number(a.distance_m)/1000:null,min=num(a.duration_sec,0)!==null?Number(a.duration_sec)/60:null;return km&&min?km/(min/60):null}
async function pairedSrpeRatios(athleteId,sport){const logs=await sb('manual_session_logs',`athlete_id=eq.${encodeURIComponent(athleteId)}&workout_id=not.is.null&rpe=not.is.null&actual_duration_min=gt.0&select=workout_id,rpe,actual_duration_min&order=created_at.desc&limit=120`).catch(()=>[]);if(!logs.length)return[];const ids=[...new Set(logs.map(x=>x.workout_id).filter(Boolean))];const[activities,workouts]=await Promise.all([sb('activities',`athlete_id=eq.${encodeURIComponent(athleteId)}&workout_id=in.(${ids.join(',')})&load=gt.0&select=workout_id,load,sport`).catch(()=>[]),sb('workouts',`athlete_id=eq.${encodeURIComponent(athleteId)}&id=in.(${ids.join(',')})&select=id,sport`).catch(()=>[])]);const wk=new Map(workouts.map(x=>[String(x.id),sportKey(x.sport)])),byWorkout=new Map();activities.forEach(x=>{if(!byWorkout.has(String(x.workout_id)))byWorkout.set(String(x.workout_id),[]);byWorkout.get(String(x.workout_id)).push(x)});const ratios=[];logs.forEach(log=>{const key=String(log.workout_id);if(sport&&wk.get(key)!==sport)return;const load=(byWorkout.get(key)||[]).reduce((sum,x)=>sum+Number(x.load||0),0),srpe=Number(log.actual_duration_min||0)*Number(log.rpe||0);if(load>0&&srpe>0)ratios.push(load/srpe)});return ratios}
async function estimateManualLoad(athleteId,payload){const duration=num(payload.duration_min,1,2000),rpe=num(payload.rpe,1,10),sport=sportKey(payload.sport);if(duration===null||rpe===null)return{load:null,source:'insufficient',confidence:'none',explanation:'Faltan duración o RPE.'};const srpe=duration*rpe,ratios=await pairedSrpeRatios(athleteId,sport);if(ratios.length>=3){const ratio=percentile(ratios,.35);return{load:Math.max(1,Math.round(srpe*ratio)),srpe,source:'estimated_athlete_srpe',confidence:ratios.length>=8?'high':'medium',samples:ratios.length,explanation:'Estimación calibrada con sesiones del mismo deportista que tienen RPE y carga objetiva.'}}const recent=await sb('activities',`athlete_id=eq.${encodeURIComponent(athleteId)}&load=gt.0&select=load,duration_sec,sport&order=activity_date.desc&limit=120`).catch(()=>[]),perMin=recent.filter(x=>sportKey(x.sport)===sport&&Number(x.duration_sec)>0).map(x=>Number(x.load)/(Number(x.duration_sec)/60)).filter(x=>Number.isFinite(x)&&x>0);if(perMin.length>=3){const base=percentile(perMin,.25),intensity=clamp(.75+(rpe-5)*.06,.55,1.05);return{load:Math.max(1,Math.round(duration*base*intensity)),srpe,source:'estimated_athlete_sport_history',confidence:perMin.length>=8?'medium':'low',samples:perMin.length,explanation:'Estimación conservadora con el cuartil bajo de carga/minuto del mismo deporte y ajuste moderado por RPE.'}}return{load:Math.max(1,Math.round(srpe/10)),srpe,source:'estimated_conservative_fallback',confidence:'low',samples:0,explanation:'Estimación provisional conservadora mientras RunFlow reúne historial propio del atleta.'}}
function similarity(a,b){const terms=[],da=activityDurationMin(a),db=activityDurationMin(b);if(da&&db)terms.push(Math.abs(Math.log(da/db)));const xa=Number(a.distance_m||0),xb=Number(b.distance_m||0);if(xa>0&&xb>0)terms.push(Math.abs(Math.log(xa/xb)));const sa=activitySpeedKmh(a),sbv=activitySpeedKmh(b);if(sa&&sbv)terms.push(Math.abs(Math.log(sa/sbv)));const ea=Number(a.elevation_gain_m||0),eb=Number(b.elevation_gain_m||0),kma=xa/1000,kmb=xb/1000;if(kma>0&&kmb>0&&(ea>0||eb>0))terms.push(Math.abs(Math.log((1+ea/kma)/(1+eb/kmb))));return terms.length?terms.reduce((sum,x)=>sum+x,0)/terms.length:999}
async function estimateZeroImportedLoad(athleteId,row,history=null){const sport=sportKey(row.sport),duration=activityDurationMin(row);if(!duration)return{load:null,source:'insufficient',confidence:'none',explanation:'Actividad sin duración suficiente.'};const sourceRows=history||await sb('activities',`athlete_id=eq.${encodeURIComponent(athleteId)}&load=gt.0&select=id,load,duration_sec,distance_m,elevation_gain_m,sport,activity_date&order=activity_date.desc&limit=200`).catch(()=>[]),candidates=sourceRows.filter(x=>sportKey(x.sport)===sport&&Number(x.duration_sec)>0&&String(x.id)!==String(row.id)).map(x=>({...x,_score:similarity(row,x)})).sort((a,b)=>a._score-b._score).slice(0,7);if(candidates.length>=3){const perHour=candidates.map(x=>Number(x.load)/(Number(x.duration_sec)/3600)).filter(x=>Number.isFinite(x)&&x>0),conservative=percentile(perHour,.35);return{load:Math.max(1,Math.round(conservative*(duration/60)*.95)),source:'estimated_comparable_history',confidence:candidates.length>=5?'medium':'low',samples:candidates.length,explanation:'Estimación con actividades comparables del mismo deporte. Duración, distancia, desnivel relativo y velocidad media intervienen en la selección; se aplica un sesgo conservador.'}}const speed=activitySpeedKmh(row)||0,km=Number(row.distance_m||0)/1000,elev=Number(row.elevation_gain_m||0),density=km>0?elev/km:0;let perMin=.25;if(sport==='bike')perMin=.28+clamp((speed-12)/60,0,.32)+clamp(density/300,0,.12);else if(sport==='walk')perMin=.16+clamp((speed-3)/25,0,.12)+clamp(density/450,0,.10);else if(sport==='run'||sport==='trail')perMin=.38+clamp((speed-7)/35,0,.22)+clamp(density/300,0,.15);else if(sport==='strength')perMin=.35;return{load:Math.max(1,Math.round(duration*clamp(perMin,.15,.85))),source:'estimated_conservative_activity',confidence:'low',samples:candidates.length,explanation:'Estimación conservadora de respaldo basada en duración, velocidad media, distancia y densidad de desnivel. Se sustituirá cuando exista una carga objetiva o mejor calibración.'}}
async function athleteActivities(athleteId,oldest,newest){const query=[`athlete_id=eq.${encodeURIComponent(athleteId)}`];if(oldest)query.push(`activity_date=gte.${encodeURIComponent(oldest)}`);if(newest)query.push(`activity_date=lte.${encodeURIComponent(`${newest}T23:59:59`)}`);query.push('select=*','order=activity_date.asc');return sb('activities',query.join('&'))}
async function reconcileAndEstimateLoads(athleteId){const rows=await athleteActivities(athleteId,null,null),imported=rows.filter(x=>!String(x.intervals_activity_id||'').startsWith('runflow-manual-')),manual=rows.filter(x=>String(x.intervals_activity_id||'').startsWith('runflow-manual-'));let deduplicated=0,estimated=0;for(const m of manual){const md=safeObj(m.raw_summary).runflow_manual||safeObj(m.raw_summary),same=imported.find(x=>{if(String(x.activity_date||'').slice(0,10)!==String(m.activity_date||'').slice(0,10))return false;if(sportKey(x.sport)!==sportKey(m.sport))return false;const dm=activityDurationMin(m),di=activityDurationMin(x);if(dm&&di&&Math.abs(dm-di)>Math.max(8,dm*.15))return false;const manualKm=Number(md.distance_km||0),importKm=Number(x.distance_m||0)/1000;if(manualKm>0&&importKm>0&&Math.abs(manualKm-importKm)>Math.max(1,manualKm*.2))return false;return true});if(same){await sb('activities',`id=eq.${encodeURIComponent(m.id)}&athlete_id=eq.${encodeURIComponent(athleteId)}`,{method:'DELETE',prefer:'return=minimal'}).catch(()=>[]);deduplicated++}}const after=deduplicated?await athleteActivities(athleteId,null,null):rows,history=after.filter(x=>Number(x.load)>0);for(const row of after){if(Number(row.load)>0||String(row.intervals_activity_id||'').startsWith('runflow-manual-'))continue;const estimate=await estimateZeroImportedLoad(athleteId,row,history);if(estimate.load===null)continue;const raw=safeObj(row.raw_summary);await sb('activities',`id=eq.${encodeURIComponent(row.id)}&athlete_id=eq.${encodeURIComponent(athleteId)}`,{method:'PATCH',body:{load:estimate.load,raw_summary:{...raw,runflow_load:{...estimate,original_imported_load:Number(row.load||0),estimated_at:new Date().toISOString()}}},prefer:'return=minimal'}).catch(()=>[]);row.load=estimate.load;history.push(row);estimated++}return{deduplicated,estimated}}
function effectiveLoad(a){const raw=Number(a.load||0),summary=safeObj(a.raw_summary);if(raw>0){const meta=safeObj(summary.runflow_load);if(meta.source)return{load:raw,source:meta.source,confidence:meta.confidence||'low'};if(summary.runflow_estimated_load)return{load:raw,source:summary.runflow_load_method||'runflow_estimated_manual',confidence:summary.runflow_load_confidence||'low'};return{load:raw,source:'imported',confidence:'high'}}return{load:0,source:'zero_unestimated',confidence:'low'}}
function computeLoadMetrics(activities,endDate=localDate()){const byDate=new Map();for(const a of activities){const d=String(a.activity_date||'').slice(0,10);if(!validDate(d))continue;const eff=effectiveLoad(a);byDate.set(d,(byDate.get(d)||0)+Number(eff.load||0))}const dates=[...byDate.keys()].sort(),start=dates[0]||addDays(endDate,-180);let fitness=0,fatigue=0;const wf=Math.exp(-1/42),wa=Math.exp(-1/7),history=[];for(let d=start;d<=endDate;d=addDays(d,1)){const load=byDate.get(d)||0;fitness=fitness*wf+load*(1-wf);fatigue=fatigue*wa+load*(1-wa);history.push({date:d,load:Math.round(load*10)/10,fitness:Math.round(fitness*100)/100,fatigue:Math.round(fatigue*100)/100,form:Math.round((fitness-fatigue)*100)/100})}return{latest:history.at(-1)||{date:endDate,load:0,fitness:0,fatigue:0,form:0},history,constants:{fitness_days:42,fatigue_days:7},source:'runflow'}}

function customFieldGet(fields,label){const row=(Array.isArray(fields)?fields:[]).find(x=>x&&x.label===label);if(!row)return null;try{return JSON.parse(row.value)}catch{return row.value}}
function customFieldSet(fields,label,value){const rows=Array.isArray(fields)?fields.filter(x=>x&&x.label!==label):[];rows.push({label,value:typeof value==='string'?value:JSON.stringify(value)});return rows.slice(0,50)}
async function profileRow(athleteId){const rows=await sb('athlete_profiles',`athlete_id=eq.${encodeURIComponent(athleteId)}&select=*&limit=1`);return rows[0]||null}
async function saveProfileFields(athleteId,patch){const current=await profileRow(athleteId);if(current){const rows=await sb('athlete_profiles',`athlete_id=eq.${encodeURIComponent(athleteId)}`,{method:'PATCH',body:{...patch,updated_at:new Date().toISOString()},prefer:'return=representation'});return rows[0]||{...current,...patch}}const rows=await sb('athlete_profiles','',{method:'POST',body:{athlete_id:athleteId,...patch,updated_at:new Date().toISOString()},prefer:'return=representation'});return rows[0]||patch}
async function listManualActivities(athleteId,oldest,newest){const rows=await athleteActivities(athleteId,oldest,newest);return rows.filter(a=>String(a.intervals_activity_id||'').startsWith('runflow-manual-'))}
async function createManualActivity(athleteId,body){const date=validDate(body.activity_date||body.date),sport=safeText(body.sport||body.type,40)||'Other',duration=num(body.duration_min,1,2000),rpe=num(body.rpe,1,10);if(!date||duration===null||rpe===null)throw Object.assign(new Error('Fecha, duración y RPE son obligatorios.'),{status:400});const estimate=await estimateManualLoad(athleteId,{duration_min:duration,rpe,sport}),load=estimate.load,id=crypto.randomUUID();const row={id,athlete_id:athleteId,workout_id:null,intervals_activity_id:`runflow-manual-${id}`,activity_date:`${date}T12:00:00Z`,sport,name:safeText(body.name||`Actividad manual · ${sport}`,160),duration_sec:Math.round(duration*60),distance_m:num(body.distance_km,0,5000)===null?null:Math.round(Number(body.distance_km)*1000),elevation_gain_m:num(body.elevation_m,0,100000),load,avg_hr:null,max_hr:null,avg_pace_sec_per_km:null,raw_summary:{source:'athlete_v2_manual',rpe,pain:num(body.pain,0,10),feeling:safeText(body.feeling,40)||null,comment:safeText(body.comment,2000),runflow_estimated_load:load,runflow_load_method:estimate.source,runflow_load_confidence:estimate.confidence,runflow_load_explanation:estimate.explanation,runflow_manual:{sport,distance_km:num(body.distance_km,0,5000),elevation_m:num(body.elevation_m,0,100000),rpe,pain:num(body.pain,0,10),feeling:safeText(body.feeling,40)||null,comment:safeText(body.comment,2000),srpe:estimate.srpe,load_source:estimate.source,confidence:estimate.confidence}}};const rows=await sb('activities','',{method:'POST',body:row,prefer:'return=representation'});return rows[0]||row}

async function findWorkoutForAthlete(athleteId,workoutId){const rows=await sb('workouts',`id=eq.${encodeURIComponent(workoutId)}&athlete_id=eq.${encodeURIComponent(athleteId)}&select=*&limit=1`);if(!rows.length)throw Object.assign(new Error('Sesión no encontrada.'),{status:404});return rows[0]}
async function conflictsForDate(athleteId,workoutId,date){const rows=await sb('workouts',`athlete_id=eq.${encodeURIComponent(athleteId)}&workout_date=eq.${date}&select=id,title,sport,planned_load,planned_duration_min`);return rows.filter(x=>String(x.id)!==String(workoutId))}
async function rescheduleWorkout(athleteId,workoutId,newDate,doUpdate){const workout=await findWorkoutForAthlete(athleteId,workoutId),date=validDate(newDate);if(!date)throw Object.assign(new Error('Fecha no válida.'),{status:400});const weekRows=await sb('training_weeks',`id=eq.${encodeURIComponent(workout.training_week_id)}&athlete_id=eq.${encodeURIComponent(athleteId)}&select=status&limit=1`);if(!weekRows.length||weekRows[0].status!=='published')throw Object.assign(new Error('El atleta solo puede mover sesiones de una semana publicada.'),{status:409});const conflicts=await conflictsForDate(athleteId,workoutId,date);if(!doUpdate)return{workout,conflicts};const oldDate=workout.workout_date,blocks=Array.isArray(workout.blocks)?workout.blocks.slice():[];let meta=blocks.find(b=>b&&b.type==='runflow_meta');if(!meta){meta={type:'runflow_meta',reschedule_history:[]};blocks.push(meta)}if(!Array.isArray(meta.reschedule_history))meta.reschedule_history=[];meta.reschedule_history.push({from:oldDate,to:date,by:'athlete',at:new Date().toISOString()});const updatedRows=await sb('workouts',`id=eq.${encodeURIComponent(workoutId)}&athlete_id=eq.${encodeURIComponent(athleteId)}`,{method:'PATCH',body:{workout_date:date,blocks,updated_at:new Date().toISOString()},prefer:'return=representation'});let intervals={synced:false,reason:'Sin evento de Intervals enlazado.'};const key=await intervalsKey(athleteId);if(key){let eventId=workout.intervals_event_id||null;if(!eventId){try{const oldest=oldDate<date?oldDate:date,newest=oldDate>date?oldDate:date,events=await intervalsFetch(key,`/athlete/0/events?oldest=${oldest}&newest=${newest}`),match=(Array.isArray(events)?events:[]).find(e=>e&&e.external_id===`runflow-workout-${workout.id}`);eventId=match?.id||null}catch{}}if(eventId){try{const event=await intervalsFetch(key,`/athlete/0/events/${encodeURIComponent(eventId)}`),payload={...event,start_date_local:`${date}T00:00:00`};delete payload.id;await intervalsFetch(key,`/athlete/0/events/${encodeURIComponent(eventId)}`,{method:'PUT',body:JSON.stringify(payload)});intervals={synced:true,event_id:String(eventId)};if(!workout.intervals_event_id)await sb('workouts',`id=eq.${encodeURIComponent(workoutId)}`,{method:'PATCH',body:{intervals_event_id:String(eventId)},prefer:'return=minimal'})}catch(e){intervals={synced:false,reason:e.message}}}}return{workout:updatedRows[0]||{...workout,workout_date:date,blocks},old_date:oldDate,new_date:date,conflicts,intervals}}
async function missedAlerts(athleteId){const today=localDate(),weeks=await sb('training_weeks',`athlete_id=eq.${encodeURIComponent(athleteId)}&status=eq.published&week_start=lte.${today}&select=id,week_start,end_date`);if(!weeks.length)return[];const ids=weeks.map(w=>w.id),workouts=await sb('workouts',`training_week_id=in.(${ids.join(',')})&athlete_id=eq.${encodeURIComponent(athleteId)}&workout_date=lt.${today}&select=id,title,workout_date,sport,planned_load,planned_duration_min,training_week_id`);if(!workouts.length)return[];const wids=workouts.map(w=>w.id),[acts,logs]=await Promise.all([sb('activities',`athlete_id=eq.${encodeURIComponent(athleteId)}&workout_id=in.(${wids.join(',')})&select=workout_id`),sb('manual_session_logs',`athlete_id=eq.${encodeURIComponent(athleteId)}&workout_id=in.(${wids.join(',')})&select=workout_id,status,created_at`)]),done=new Set(acts.map(a=>String(a.workout_id)));for(const l of logs)if(['completed','partial'].includes(l.status))done.add(String(l.workout_id));return workouts.filter(w=>!done.has(String(w.id))).map(w=>({id:`missed-${w.id}`,type:'missed_session',severity:'warning',workout_id:w.id,date:w.workout_date,title:w.title,message:`Sesión prevista no realizada: ${w.title}`,action:'review_workout'}))}

async function getBanner(athleteId){const profile=await profileRow(athleteId);return customFieldGet(profile?.custom_fields,BANNER_FIELD)}
async function setBanner(athleteId,body){const profile=await profileRow(athleteId),banner=body&&body.active===false?null:{text:safeText(body.text,500),tone:safeText(body.tone,30)||'motivation',active:true,updated_at:new Date().toISOString()};if(banner&&!banner.text)throw Object.assign(new Error('Escribe el mensaje motivacional.'),{status:400});const fields=customFieldSet(profile?.custom_fields,BANNER_FIELD,banner);await saveProfileFields(athleteId,{custom_fields:fields});return banner}
async function maxLoadHistory(athleteId){const profile=await profileRow(athleteId);return customFieldGet(profile?.custom_fields,LOAD_HISTORY_FIELD)||[]}
async function addMaxLoadHistory(athleteId,body){const value=num(body.value,0,10000),effective=validDate(body.effective_date)||localDate();if(value===null)throw Object.assign(new Error('Carga máxima no válida.'),{status:400});const profile=await profileRow(athleteId),history=Array.isArray(customFieldGet(profile?.custom_fields,LOAD_HISTORY_FIELD))?customFieldGet(profile?.custom_fields,LOAD_HISTORY_FIELD):[];history.push({value,effective_date:effective,created_at:new Date().toISOString()});history.sort((a,b)=>String(a.effective_date).localeCompare(String(b.effective_date)));const fields=customFieldSet(profile?.custom_fields,LOAD_HISTORY_FIELD,history),tolerance={...safeObj(profile?.load_tolerance_profile),provisional_ceiling:value,updated_at:new Date().toISOString()};await saveProfileFields(athleteId,{custom_fields:fields,load_tolerance_profile:tolerance});return history}

const PLAN_CHANGE_FIELD='RUNFLOW_PLAN_CHANGE_HISTORY';
async function dynamicProfile(athleteId){const p=await profileRow(athleteId)||{},availability=safeObj(p.availability),tolerance=safeObj(p.load_tolerance_profile);return{availability,strength_mode:availability.strength_mode||'runflow',external_strength_days:Array.isArray(availability.external_strength_days)?availability.external_strength_days:[],max_load:tolerance.provisional_ceiling??null,load_tolerance_profile:tolerance,load_ceiling_history:customFieldGet(p.custom_fields,LOAD_HISTORY_FIELD)||[],change_history:customFieldGet(p.custom_fields,PLAN_CHANGE_FIELD)||[],updated_at:p.updated_at||null}}
async function saveDynamicProfile(athleteId,body,coachUserId){const p=await profileRow(athleteId)||{},previous=await dynamicProfile(athleteId),availability={...safeObj(p.availability),...safeObj(body.availability)};if(['runflow','external'].includes(body.strength_mode))availability.strength_mode=body.strength_mode;if(Array.isArray(body.external_strength_days))availability.external_strength_days=body.external_strength_days.map(Number).filter(x=>Number.isInteger(x)&&x>=1&&x<=7);let fields=Array.isArray(p.custom_fields)?p.custom_fields:[],tolerance={...safeObj(p.load_tolerance_profile)};if(Object.prototype.hasOwnProperty.call(body,'max_load')){const ceiling=num(body.max_load,0,10000);tolerance.provisional_ceiling=ceiling;if(Number(previous.max_load)!==Number(ceiling)){const history=Array.isArray(customFieldGet(fields,LOAD_HISTORY_FIELD))?customFieldGet(fields,LOAD_HISTORY_FIELD):[];history.push({effective_date:validDate(body.effective_date)||localDate(),from:previous.max_load??null,to:ceiling,coach_user_id:coachUserId,created_at:new Date().toISOString()});fields=customFieldSet(fields,LOAD_HISTORY_FIELD,history.slice(-100))}}const changed=JSON.stringify(previous.availability||{})!==JSON.stringify(availability)||previous.strength_mode!==availability.strength_mode||JSON.stringify(previous.external_strength_days||[])!==JSON.stringify(availability.external_strength_days||[])||Number(previous.max_load)!==Number(tolerance.provisional_ceiling);if(changed){const hist=Array.isArray(customFieldGet(fields,PLAN_CHANGE_FIELD))?customFieldGet(fields,PLAN_CHANGE_FIELD):[];hist.push({effective_date:validDate(body.effective_date)||localDate(),type:'athlete_profile_change',coach_user_id:coachUserId,created_at:new Date().toISOString()});fields=customFieldSet(fields,PLAN_CHANGE_FIELD,hist.slice(-100))}const saved=await saveProfileFields(athleteId,{availability,load_tolerance_profile:tolerance,custom_fields:fields});return{profile:saved,dynamic:await dynamicProfile(athleteId),replan_recommended:changed}}
function dayAvailability(profile,iso){const days=profile?.availability?.days,wd=((new Date(`${iso}T12:00:00Z`).getUTCDay()+6)%7)+1;if(Array.isArray(days))return days.find(x=>Number(x.day)===wd)||null;return null}
function startOfWeekIso(iso){const d=new Date(`${iso}T12:00:00Z`),day=d.getUTCDay()||7;d.setUTCDate(d.getUTCDate()-day+1);return d.toISOString().slice(0,10)}
async function replanFuture(athleteId,body,coachUserId){const effective=validDate(body.effective_date)||localDate(),profile=await dynamicProfile(athleteId),weeks=await sb('training_weeks',`athlete_id=eq.${encodeURIComponent(athleteId)}&status=eq.draft&week_start=gte.${startOfWeekIso(effective)}&select=*&order=week_start.asc`).catch(()=>[]);let moved=0,blocked=0,unchanged=0;const changes=[];for(const week of weeks){const workouts=await sb('workouts',`athlete_id=eq.${encodeURIComponent(athleteId)}&training_week_id=eq.${encodeURIComponent(week.id)}&select=*&order=workout_date.asc`).catch(()=>[]),occupied=new Set(workouts.filter(w=>String(w.workout_date).slice(0,10)<effective).map(w=>String(w.workout_date).slice(0,10)));for(const w of workouts){const original=String(w.workout_date).slice(0,10);if(original<effective){unchanged++;continue}const current=dayAvailability(profile,original),duration=num(w.planned_duration_min,0,2000),compatible=dayAllowsWorkout(current,w,profile)&&(!current||current.max_minutes==null||duration==null||duration<=Number(current.max_minutes));if(compatible&&!occupied.has(original)){occupied.add(original);unchanged++;continue}let chosen=null;for(let off=0;off<7;off++){const d=addDays(week.week_start,off);if(d<effective||occupied.has(d))continue;const a=dayAvailability(profile,d);if(!dayAllowsWorkout(a,w,profile))continue;if(duration!=null&&a?.max_minutes!=null&&duration>Number(a.max_minutes))continue;chosen=d;break}if(chosen){await sb('workouts',`id=eq.${encodeURIComponent(w.id)}&athlete_id=eq.${encodeURIComponent(athleteId)}`,{method:'PATCH',body:{workout_date:chosen,updated_at:new Date().toISOString()},prefer:'return=minimal'});occupied.add(chosen);moved++;changes.push({workout_id:w.id,title:w.title,from:original,to:chosen})}else{blocked++;changes.push({workout_id:w.id,title:w.title,from:original,to:null,reason:'No existe un hueco compatible en este microciclo.'})}}}const p=await profileRow(athleteId)||{};let fields=Array.isArray(p.custom_fields)?p.custom_fields:[],hist=Array.isArray(customFieldGet(fields,PLAN_CHANGE_FIELD))?customFieldGet(fields,PLAN_CHANGE_FIELD):[];hist.push({effective_date:effective,type:String(body.reason||'replan_future'),moved,blocked,coach_user_id:coachUserId,changes,created_at:new Date().toISOString()});fields=customFieldSet(fields,PLAN_CHANGE_FIELD,hist.slice(-100));await saveProfileFields(athleteId,{custom_fields:fields});return{effective_date:effective,weeks:weeks.length,moved,blocked,unchanged,changes,note:'Solo se han recolocado sesiones de semanas futuras en borrador. El pasado y las semanas publicadas permanecen intactos. Los cambios de objetivo o de dosis que exigen reconstruir fases/mesociclos se dejan marcados para revisión guiada; no se inventan duraciones de fase no definidas por la metodología.'}}

async function handle(req,res,url){const method=req.method||'GET',p=url.pathname;if(!p.startsWith('/api/v9/')&&!p.startsWith('/api/v2/'))return false;const session=await requireSession(req,res);if(p==='/api/v9/library'&&method==='GET'){requireRole(session,'coach');const lib=loadLibrary();sendJson(res,200,{...lib,count:lib.sessions.length});return true}let m=p.match(/^\/api\/v9\/coach\/athletes\/([^/]+)\/alerts$/);if(m&&method==='GET'){const id=decodeURIComponent(m[1]);await ensureCoachAccess(session,id);sendJson(res,200,{alerts:await missedAlerts(id)});return true}m=p.match(/^\/api\/v9\/coach\/athletes\/([^/]+)\/banner$/);if(m){const id=decodeURIComponent(m[1]);await ensureCoachAccess(session,id);if(method==='GET'){sendJson(res,200,{banner:await getBanner(id)});return true}if(method==='PUT'){sendJson(res,200,{banner:await setBanner(id,await readJson(req))});return true}}m=p.match(/^\/api\/v9\/coach\/athletes\/([^/]+)\/load-metrics$/);if(m&&method==='GET'){const id=decodeURIComponent(m[1]);await ensureCoachAccess(session,id);const oldest=validDate(url.searchParams.get('oldest'))||addDays(localDate(),-365),newest=validDate(url.searchParams.get('newest'))||localDate();await reconcileAndEstimateLoads(id);const acts=await athleteActivities(id,oldest,newest),metrics=computeLoadMetrics(acts,newest);sendJson(res,200,{...metrics,activities:acts.map(a=>({...a,runflow_load:effectiveLoad(a)}))});return true}m=p.match(/^\/api\/v9\/coach\/athletes\/([^/]+)\/max-load-history$/);if(m){const id=decodeURIComponent(m[1]);await ensureCoachAccess(session,id);if(method==='GET'){sendJson(res,200,{history:await maxLoadHistory(id)});return true}if(method==='POST'){sendJson(res,201,{history:await addMaxLoadHistory(id,await readJson(req))});return true}}m=p.match(/^\/api\/v9\/coach\/athletes\/([^/]+)\/dynamic-profile$/);if(m){const id=decodeURIComponent(m[1]);await ensureCoachAccess(session,id);if(method==='GET'){sendJson(res,200,await dynamicProfile(id));return true}if(method==='PUT'){sendJson(res,200,await saveDynamicProfile(id,await readJson(req),session.user.id));return true}}m=p.match(/^\/api\/v9\/coach\/athletes\/([^/]+)\/replan$/);if(m&&method==='POST'){const id=decodeURIComponent(m[1]);await ensureCoachAccess(session,id);sendJson(res,200,await replanFuture(id,await readJson(req),session.user.id));return true}if(p==='/api/v2/athlete/banner'&&method==='GET'){requireRole(session,'athlete');sendJson(res,200,{banner:await getBanner(session.athlete_id)});return true}if(p==='/api/v2/athlete/manual-activities'){requireRole(session,'athlete');if(method==='GET'){const oldest=validDate(url.searchParams.get('oldest'))||addDays(localDate(),-180),newest=validDate(url.searchParams.get('newest'))||localDate();sendJson(res,200,{activities:await listManualActivities(session.athlete_id,oldest,newest)});return true}if(method==='POST'){sendJson(res,201,{activity:await createManualActivity(session.athlete_id,await readJson(req))});return true}}if(p==='/api/v2/athlete/load-metrics'&&method==='GET'){requireRole(session,'athlete');const oldest=validDate(url.searchParams.get('oldest'))||addDays(localDate(),-365),newest=validDate(url.searchParams.get('newest'))||localDate();await reconcileAndEstimateLoads(session.athlete_id);const acts=await athleteActivities(session.athlete_id,oldest,newest);sendJson(res,200,computeLoadMetrics(acts,newest));return true}m=p.match(/^\/api\/v2\/athlete\/workouts\/([^/]+)\/reschedule$/);if(m&&method==='POST'){requireRole(session,'athlete');const body=await readJson(req),result=await rescheduleWorkout(session.athlete_id,decodeURIComponent(m[1]),body.new_date,body.preview!==true);sendJson(res,body.preview===true?200:201,result);return true}sendJson(res,404,{error:'Ruta V9/V2 no encontrada.'});return true}

const originalCreateServer=http.createServer;http.createServer=function patchedCreateServer(listener){return originalCreateServer.call(http,async(req,res)=>{const url=new URL(req.url,`http://${req.headers.host||'localhost'}`);if(!url.pathname.startsWith('/api/v9/')&&!url.pathname.startsWith('/api/v2/'))return listener(req,res);try{await handle(req,res,url)}catch(error){console.error('[v9-engine-hook]',error);sendJson(res,Number(error.status||500),{error:error.message||'Error interno.',details:error.details||null})}})};
